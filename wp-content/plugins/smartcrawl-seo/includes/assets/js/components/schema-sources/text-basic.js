import {__} from '@wordpress/i18n';

const Text = {
	id: 'Text',
	sources: {
		post_meta: {
			label: __('Post Meta', 'wds')
		},
		custom_text: {
			label: __('Custom Text', 'wds')
		},
	}
};
export default Text;
