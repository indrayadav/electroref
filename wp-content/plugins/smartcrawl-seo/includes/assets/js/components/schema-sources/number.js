import {__} from '@wordpress/i18n';

const Number = {
	id: 'Number',
	sources: {
		post_meta: {
			label: __('Post Meta', 'wds')
		},
		number: {
			label: __('Custom Number', 'wds')
		},
	}
};
export default Number;
